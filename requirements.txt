# =============================================================================
# PyWallet — Python Dependencies
# Install with: pip install -r requirements.txt
# =============================================================================

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Database — ORM + async driver
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Configuration management
pydantic-settings==2.6.1
pydantic[email]==2.10.2
python-dotenv==1.0.1

# Cache + message broker
redis[hiredis]==5.2.1

# Background tasks
celery==5.4.0
flower==2.0.1

# Auth (Phase 2)
# PyJWT: chosen over python-jose — actively maintained, simpler API, no sub-dependencies.
# python-jose is removed because PyJWT is explicitly requested and is the modern standard.
PyJWT==2.9.0
#
# bcrypt: used DIRECTLY (no passlib wrapper) because passlib 1.7.4 has a known
# incompatibility with bcrypt >= 4.0 (detect_wrap_bug internal API change).
# Direct bcrypt is identical in behavior — same algorithm, same hash format.
# Pinned to 4.2.1 (latest stable with stable API).
bcrypt==4.2.1

# HTTP client (used in tests and healthchecks)
httpx==0.27.2

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0

# Rate limiting
slowapi==0.1.9

# Structured logging
structlog==24.4.0

# Utilities
python-multipart==0.0.12   # Required for FastAPI form data / file uploads
